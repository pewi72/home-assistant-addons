name: Hello World Test
description: "Minimal test addon to verify local addon visibility"
version: "1.0"
slug: hello_world_test
startup: application
boot: auto
init: false
homeassistant_api: false

options:
  test_option: "default"

schema:
  test_option: str
